fastapi>=0.110.0
uvicorn[standard]>=0.29.0
# FunPayAPI 1.1.0 жёстко пинит requests==2.28.1 и requests_toolbelt==0.10.1,
# поэтому совместимые верхние границы держим намеренно широкими, чтобы пакет
# можно было поставить и в Electron-сборку, и в Docker-образ.
requests>=2.28.1,<3
requests-toolbelt>=0.10.1,<2
beautifulsoup4>=4.12.0
lxml>=5.1.0
websockets>=12.0
pydantic>=2.0
# FunPayAPI ставится только в Docker-сборке VPS-воркера. В Electron-сборке
# она поставляется отдельно (через extraResources), поэтому pip-install
# здесь нужен только для VPS-режима.
FunPayAPI>=1.1.0
